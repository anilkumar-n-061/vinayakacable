apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deploy-app
  labels: my-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: devops-app-c5
        image: anilkumar061/vinayakacable:latest
        ports:
        - containerPort: 8000
--- 
apiVersion: v1
kind: Service
metadata:
  name: devops-service-v6
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30326

